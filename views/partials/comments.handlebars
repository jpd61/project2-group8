<div class="comments">
  {{#each this}}
  <section class="comment bg-light m-3 p-3 rounded">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>